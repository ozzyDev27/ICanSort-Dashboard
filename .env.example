# Optional — add your OpenAI API key to enable the AI chat assistant.
# Without it, the chatbot uses a smart local fallback that reads from the data.
# Copy this file to .env and fill in your key.
VITE_OPENAI_KEY=sk-...
